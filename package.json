{
  "name": "static-video-gallery",
  "version": "2.0.0",
  "description": "Static video gallery with WebP thumbnail previews",
  "main": "generate-thumbs.js",
  "scripts": {
    "build": "node generate-thumbs.js && echo 'Build complete'",
    "thumbs": "node generate-thumbs.js",
    "dev": "npm run thumbs && npx http-server . -p 8080 -o",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "video",
    "gallery",
    "webp",
    "thumbnails",
    "static-site"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 